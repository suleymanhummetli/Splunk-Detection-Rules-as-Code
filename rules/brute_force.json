{
  "name": "Detect_Brute_Force_Stat",
  "search": "index=api_logs (status=401 OR status=404) | stats count as failures by src_ip | where failures > 30",
  "description": "Detects IPs with high failure rate (30+ in 1 min)",
  "earliest_time": "-5m",
  "latest_time": "now",
  "cron_schedule": "*/1 * * * *",
  "alert.suppress": "0",
  "alert.digest_mode": "0",
  "action.script.filename": "sdp_ticket.py"
}
